#include<stdio.h>
main()
{
	char sy;
	int su,gokei,heikin;
	printf("ˆ—‚ð“ü—Í:");
	scanf("%c", &sy);
	printf("®”‚ð“ü—Í:");
	scanf("%d", &su);
	if (sy >= 'A' && sy <= 'Z' || sy >= 'a' && sy <= 'z'){
		switch (){
			case
		default:
			break;
		}
	}
	else{
		printf("ƒGƒ‰[");
	}
}